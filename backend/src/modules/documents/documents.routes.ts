import {Router} from "express"; import {requireAuth} from "../../common/middleware/auth.js"; import {validate} from "../../common/middleware/validate.js"; import {documentsController} from "./documents.controller.js"; import {createDocumentSchema,documentIdSchema,documentQuerySchema} from "./documents.validation.js";
const router:Router=Router(); router.get("/",requireAuth,validate({query:documentQuerySchema}),documentsController.list); router.post("/",requireAuth,validate(createDocumentSchema),documentsController.create); router.get("/:id",requireAuth,validate({params:documentIdSchema}),documentsController.get); router.delete("/:id",requireAuth,validate({params:documentIdSchema}),documentsController.remove); export default router; export {router as documentsRoutes};
