import { ForbiddenError, NotFoundError } from "../../common/errors/AppError.js"; import { documentsRepository } from "./documents.repository.js"; import type { CreateDocumentInput, DocumentQuery } from "./documents.validation.js";
export class DocumentsService { list(q:DocumentQuery, actor:{userId:string;role:string}) { if(actor.role === "member" && (q.owner_type !== "user" || q.owner_id !== actor.userId)) throw new ForbiddenError("Members may only access their own documents"); return documentsRepository.list(q); } async get(id:string, actor:{userId:string;role:string}) { const d=await documentsRepository.find(id); if(!d) throw new NotFoundError("Document was not found"); if(actor.role === "member" && (d.owner_type !== "user" || d.owner_id !== actor.userId)) throw new ForbiddenError("You may only access your own documents"); return d; } async create(i:CreateDocumentInput, actor:{userId:string;role:string}) { if(actor.role === "member" && (i.owner_type !== "user" || i.owner_id !== actor.userId)) throw new ForbiddenError("Members may only attach their own documents"); return documentsRepository.create(i,actor.userId); } async remove(id:string,actor:{userId:string;role:string}) { const d=await this.get(id,actor); if(actor.role === "member" && d.uploaded_by !== actor.userId) throw new ForbiddenError("Only the uploader or finance staff may delete a document"); return documentsRepository.remove(id); } }
export const documentsService = new DocumentsService();
